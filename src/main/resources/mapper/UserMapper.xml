<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sy4.mapper.UserMapper">

    <resultMap id="userMap" type="User">
        <id column="id" property="id" />
        <result column="login_name" property="loginName" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="type" property="type" />
    </resultMap>

    <select id="findByLoginName" resultMap="userMap">
        SELECT * FROM user WHERE login_name = #{loginName}
    </select>

    <select id="findById" resultMap="userMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(login_name, password, name, phone, email, type)
        VALUES(#{loginName}, #{password}, #{name}, #{phone}, #{email}, #{type})
    </insert>

    <update id="update">
        UPDATE user SET name = #{name}, phone = #{phone}, email = #{email}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findAll" resultMap="userMap">
        SELECT * FROM user ORDER BY id
    </select>

    <select id="search" resultMap="userMap">
        SELECT * FROM user
        <where>
            <if test="name != null and name != ''">
                name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        ORDER BY id
    </select>

</mapper>
